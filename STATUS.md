# STATUS

_Updated 2026-09-24._

## Completed
- Public home for Louis's skills, starting with the nine `gooey-section-*` skills (moved from the private labs repo).
- Laid out by category: `skills/<category>/<skill>/` with `SKILL.md`, `REFERENCES.md` (links only), `agents/openai.yaml`, `assets/`, `references/` and a `demo/` (`index.html`, `PROMPT.md`, 1280×720 `preview.jpg`). Category README for `sections`; root README with library list, folder contract and install; DEMOS.md, SCREENSHOTS.md/.html and a hero image, all generated by `scripts/`.
- `scripts/validate-skills.cjs` checks the contract (names, frontmatter, openai.yaml, links-only references, demo files and preview size, private paths, engine copies). It caught all five faults planted in a test copy.

## Current
- Louis is testing the gooey-section skills in real builds.

## Decisions
- New categories (for example `ui`) are created with their first skill, not as empty folders.
- The Slosh preview shows a one-pixel notch where the curve crosses the boundary. The original lab draws the same notch, so the port keeps it.

## Next
- Add more skills; run `build-previews`, `build-gallery` and `validate-skills`, then `python3 install.py`.
